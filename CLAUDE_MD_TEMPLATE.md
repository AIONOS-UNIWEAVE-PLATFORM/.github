# CLAUDE.md Template\n\nCopy the block below into your repo root as `CLAUDE.md`. Replace placeholder text in `[brackets]`. Delete these instructions when done.\n\n---\n\n```markdown\n# CLAUDE.md\n\n## What this service does\n\n[Service name] is [what it does in one sentence]. It [key responsibility]. It is part of the UniWeave platform and [how it fits].\n\n## How to run locally\n\nPrerequisites:\n- [Runtime]: [version] (e.g., Node.js 20+, Python 3.10+, Java 17)\n- [Database]: [version] (e.g., MongoDB 7.0, PostgreSQL 15)\n- [Other]: [e.g., Kafka running locally, Redis, etc.]\n\n```bash\n# Install dependencies\n[npm install / pip install -r requirements.txt / mvn clean install]\n\n# Set up environment\ncp .env.example .env\n# Edit .env with your local values\n\n# Run\n[npm run dev / python app.py / mvn spring-boot:run]\n\n# The service starts on port [XXXX]\n```\n\n## How to test\n\n```bash\n# Run all tests\n[npm test / pytest / mvn test]\n\n# Run specific test file\n[npm test -- path/to/test / pytest path/to/test.py]\n\n# Linting\n[npm run lint / ruff check . / N/A]\n```\n\n## Key files\n\n| File | What it does |\n|---|---|\n| `[src/index.ts]` | [Entry point, starts the server] |\n| `[src/routes/api.ts]` | [All API route definitions] |\n| `[src/services/engine.ts]` | [Core business logic for X] |\n| `[src/config/index.ts]` | [Configuration loading from env vars] |\n| `[src/models/schema.ts]` | [Database models / schemas] |\n\n## Environment variables\n\n| Variable | Purpose |\n|---|---|\n| `[AZURE_OPENAI_ENDPOINT]` | [Azure OpenAI API endpoint] |\n| `[AZURE_OPENAI_KEY]` | [API key for Azure OpenAI] |\n| `[MONGODB_URI]` | [MongoDB connection string] |\n| `[KAFKA_BOOTSTRAP_SERVERS]` | [Kafka broker addresses] |\n| `[PORT]` | [Server port, default XXXX] |\n\n> Never put actual values here. Only variable names and descriptions.\n\n## Architecture notes\n\n[2-5 sentences about the service architecture. Include:]\n- [What pattern it follows (REST API, event-driven, WebSocket, etc.)]\n- [Key libraries/frameworks (Express, FastAPI, Spring Boot, etc.)]\n- [Data flow: what comes in, what goes out]\n- [Any important design decisions]\n\n## Dependencies on other services\n\n| Service | How we depend on it | Required for local dev? |\n|---|---|---|\n| `[api-gateway]` | [Routes traffic to this service] | [No — call directly on local port] |\n| `[MongoDB]` | [Stores audit records] | [Yes — run locally or use shared dev] |\n| `[Kafka]` | [Publishes events to audit-queue] | [Yes — run locally via Docker] |\n```\n\n---\n\n## Instructions\n\n1. Copy everything between the ``` fences above into your repo as `CLAUDE.md`\n2. Replace every `[bracketed]` placeholder with actual values\n3. Delete any sections that genuinely don't apply\n4. Keep it under 100 lines\n5. Open a PR with the title: `chore: add CLAUDE.md for repo onboarding`\n\n## What makes a good CLAUDE.md\n\n- **Accurate** — if it says `npm test` runs tests, it actually does\n- **Concise** — an engineer (or Claude) should understand the service in 2 minutes\n- **Current** — update it when things change, PR like code\n- **No secrets** — env var names only, never values